# Secrets — never commit real keys (docs/security.md)
.env
.env.*
!.env.example
# app/lib/config/env.dart only reads --dart-define values and IS committed.
# Put any local key file here instead:
app/lib/config/env.local.dart
supabase/.temp/
supabase/.branches/

# The Flutter web build copied beside the site for hosting
website/app/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.iml

# Node / Deno caches
node_modules/
.deno/
